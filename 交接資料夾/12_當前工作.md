# 當前工作

- 專案代號：`PRJ-20260720-YLM13Q0D`
- 工作代號：`WRK-20260720-408T2R4T`
- 文件版本：`20`
- 最後更新時間：`2026-07-24T16:27:32+08:00`
- 最後更新員工：`EMP-Codex-20260720-11018OGZ`
- 已同步至日誌：`LOG-0028`

> 下一個行動的唯一正式來源。每次交接更新；不要重複完整背景、長期規則或工作歷史。

## 目前目標與進度

`mvproduct0721` 現有 9 張非重複 JSP 標註圖，作為批次前試作與回歸基準。內部六類批次閘門已通過；等待使用者確認圖片本身，確認前批次維持禁止。

## 本輪已確認要求

- EL 候選 A：將 EL 原始字形本身改成粉紅色。
- EL 候選 B：保留 EL 原始字形，另加粉紅色下劃線。
- 候選 A 的 QA 檢查字形顏色、漏色像素與誤染鄰字；候選 B 的 QA 檢查原字未變色、線的位置、端點與鄰字。
- 不得把兩套互斥的 EL QA 同時套在同一張圖；每次測試結果必須標明候選方案。
- 圖例固定兩欄八類。左欄：HTML、CSS、JSP 指令／JSP 註解、JSTL；右欄：JSP Action、Java scriptlet、EL、JavaScript。即使未使用某類也須保留。
- `mvweb0717` 已完成原始截圖，不得再記為未完成、暫緩或阻塞。
- `99_歷史版本_勿看` 的實際位置為 `C:\workspace202607\學習筆記JavaWeb\picture\02_標註成品\00_樣板待確認\99_歷史版本_勿看`，保留原位。
- 0717 採粉紅字形染色。新流程固定行距 19 px；一般頁第一行中心 106.5，拉到底頁第一行中心 112.5，BOTTOM 偏差 +6 px。未修改 Java Web 原始碼或既有原圖。
- 使用者已確認未來正式根目錄只保留同層的 `交接資料夾`、`作圖系統`、`picture` 與各 Java Web 專案；`作圖系統＋picture`將取代原本的`學習筆記JavaWeb`容器。
- `JavaWeb學習相關`只是先前導入作圖系統使用的三份輔助文件，不是 JavaWeb 知識庫，也不是最終正式資料夾。
- 正式遷移須等待交接系統 1.11 與作圖系統 0.4 完成後一次處理；遷移核對前不得直接刪除舊容器，先標示為舊容器、禁止使用，待使用者確認後再決定是否刪除。

## 未完成、阻塞與待決定

- 跨專案的唯一正式 EL 方案仍未定案；0717 已依使用者本輪指示限定使用粉紅字形。
- 作圖系統候選為 `CAND-EL-001／EL 粉紅字形` 與 `CAND-EL-002／EL 原字＋粉紅下劃線`；兩者均為候選測試中。
- 目前沒有跨專案正式確認的唯一樣板；作圖系統已確認樣板版本為無。
- 原始截圖盤點與補截已完成；沒有未完成或無法處理的工作範圍來源。`Servers`、部署資料、Git／系統資料、交接與成果資料均已排除。
- 預設 `pwsh.exe`／WindowsApps 命令環境曾以結束碼 `-2146233082` 與 CET 錯誤終止；不得反覆重試，也不得修改執行政策、安全設定或系統組態。目前以可用的替代讀取方式工作，此項不阻擋規則文件整理。
- 已知技術限制仍有效：固定像素字寬推算與使用同一套錯誤定位座標進行自我 QA 均禁止。
- 本輪已建立可用的獨立字元證據：以本機 OCR 從各自原圖取得 `$`、`{`、`}`及鄰字框，再以像素差異確認目標完整改色、鄰字變動為 0；這不會自動把先前 0717 的失效 QA 改為通過。
- EL 的可重複方法已寫入作圖系統 `工作資料/方法/EL粉紅字形_OCR邊界與獨立QA.md`；說明對應改用同色編號，方法位於 `工作資料/方法/說明對應_無交錯引線.md`。
- 不同色框線不得共線、重疊或貼合；安全空間不足時使用明確括號，禁止不完整矩形。同類每頁預設只保留一個代表編號；長檔後續頁必須新增來源內容。規則已寫入 `JWEB-RULE-013`～`015`。
- 批次必須通過來源／分頁、語法分類、標記幾何、EL 證據、成果分流及回歸案例六類閘門，並取得使用者對試作的確認。新型錯誤必須先撤銷 PASS、停止批次、補規則與回歸，再恢復生產；規則位於 `JWEB-RULE-016`、`017`。
- 根目錄重整尚未執行。待取得交接系統 1.11 與作圖系統 0.4 後，逐項確認舊`00_請先讀我.md`、`01_JavaWeb學習交接.md`及`JavaWeb學習相關`三份導入文件的有效內容已被正式系統吸收，再配合升級遷移。

## 下一步

- 等待使用者提供交接系統 1.11 與作圖系統 0.4；取得後依已確認的根目錄結構配合升級，一次完成作圖系統、SOP、picture 與必要引用的遷移。
- 遷移時歷史日誌舊路徑保持原文；有效現況進交接系統，作圖規格／SOP／候選／契約／QA方法進作圖系統，圖片素材與成果進 picture。
- 使用者檢視 `C:\workspace202607\學習筆記JavaWeb\picture\02_標註成品\01_未驗收\mvproduct0721` 的 9 張新版。
- 使用者確認後，才可依 `批次生產_進入閘門與回歸QA.md` 重新核對並開放後續批次。
- 使用者確認前不得移入已驗收成品；跨專案唯一正式 EL 方案仍未定案。

## 本輪成果狀態

- 成果位置：`C:\workspace202607\學習筆記JavaWeb\picture`
- 本輪修訂：從基底重新合成 9 張圖片；舊 10 張及重複頁可復原地移至內部 QA 歷程。
- 內部 QA：使用者入口只有 9 張 PNG。`productlist` 第 1 頁涵蓋第 1–47 行，第 2 頁涵蓋第 41–87 行並新增第 48–87 行；重複頁與缺頁均為 0。編號互疊、同類重複編號、不完整矩形、標記碰撞與過近均為 0；66 個唯一 EL 全部通過。
- 本輪制度更新沒有製作、修改、移動或重新驗收圖片；只建立批次進入閘門與 REG-01～REG-10 回歸清單。
- 本輪只讀回顧兩套系統並確認未來根目錄架構；沒有搬移、刪除、改名或修改作圖系統、SOP、picture、Java Web 專案與圖片成果。
- 原始截圖總數與既有清冊保持不變；本次升級未重驗或搬移圖片成果。
- 先前 9 張錯誤成果及 QA 位於內部 `WITHDRAWN_0717_背景誤染`；未驗收入口已清空。三張 r01 已永久刪除；三張 r02 仍存在但狀態為 `QA_UNRELIABLE`，不得作為樣板或通過成果。
- 作圖系統詳細入口：`C:\workspace202607\學習筆記JavaWeb\作圖系統\00_先讀_狀態與啟動.md`
- 歷史版本位置：`C:\workspace202607\學習筆記JavaWeb\picture\02_標註成品\00_樣板待確認\99_歷史版本_勿看`，維持原位。
