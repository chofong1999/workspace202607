# 先讀：身分與啟動

> 一個AI對話是一名員工。修改成果前必須先接手；只查看時不登記、不改狀態。
> 本制度的現役制度檔以`system_manifest.json`為準；同功能的舊制度檔即使仍存在，也不得同時生效。

## 工作身分

- 交接制度版本：1.10
- 專案代號：`PRJ-20260720-YLM13Q0D`
- 專案名稱：`workspace202607 Java Web 學習資料`
- 工作代號：`WRK-20260720-408T2R4T`
- 工作名稱：`Eclipse 程式碼截圖語言標註圖片製作`
- 一句話說明：`從本機 Eclipse 擷取 Java Web 程式碼，依現行標註規範與 SOP 製作並檢查語言分層教學圖片。`

## 目前狀態

- 交接狀態：`可接續`
- 交接世代：`28`
- 目前工作者：`無`
- 本輪開始時間：
- 最新日誌：`LOG-0028`
- 下一日誌：`LOG-0029`
- 成果位置：`C:\workspace202607\學習筆記JavaWeb\picture`
- 成果版本識別：`無`
- 最近成功交接時間：`2026-07-24T16:27:32+08:00`

狀態只使用：`未初始化`、`建立中`、`可接續`、`工作中`、`待核對`、`維護中`、`已停止`。

## 功能、指令與條件

訊息第一個非空白字元為全形`！`或半形`!`，且第一段文字正好是下列功能名稱時，視為明確指令；參數以空白分隔。沒有前置符號時依自然語言與狀態選擇同一套流程；句尾驚嘆號不算指令。

| 功能 | 可執行條件與流程 |
|---|---|
| `首次交接` | 只限`未初始化`；讀`80_按需/00_首次交接.md`，否則停止 |
| `接手` | 只限`可接續`；讀`01_日常接手與交接.md`。同一對話已在工作中只回報現況，其他工作者存在時拒絕 |
| `交接` | `工作中`時只允許目前工作者依`01`交接；`未初始化`時轉首次交接；已可接續時只回報已完成 |
| `只查看` | 只讀要求所需資料，不登記或改狀態 |
| `檢查狀態` | 只讀本文件與`使用者_目前狀態與待辦.md`，不接手 |
| `升級版本` | 已提供新版，且現有來源已指定或能確認不存在；讀新版`80_按需/30_版本升級.md`自動辨識。正式來源須可接續且沒有工作者 |
| `更新知識庫` | 已指定知識套件且沒有其他AI正在修改；讀`80_按需/20_知識庫管理.md` |
| `封存日誌` | 只限可接續且沒有工作者；讀`80_按需/10_異常處理.md`的「手動封存」 |

指令只選定功能，不跳過確認、權限或其他流程規則。條件不符時不修改，也不猜成其他功能；只回報指令、唯一不符條件及一個下一步。未知指令不執行，只列出可用指令。除了未初始化的`交接`轉首次交接，以及`升級版本`的來源辨識外，不自動改做另一個寫入功能。

## 外部子系統要求寫入

若同一工作使用作圖、測試或其他子系統，其寫入要求仍受本制度管理。子系統可在第一次正式寫入前自動依本制度取得工作權；只讀操作不接手，已是目前工作者時不重複接手，其他工作者存在或狀態不允許時停止。子系統不得藉自動接手修改或放寬本制度。

## 非預期問題

準備重試、深入調查或詢問使用者前，先用原始錯誤碼、特殊錯誤句及失敗元件搜尋`80_按需/知識庫/00_知識庫入口.md`指定的索引；正常時不得閱讀知識庫。沒有匹配再讀`80_按需/10_異常處理.md`。

## 離開時

依`01_日常接手與交接.md`完成交接。只有狀態為`可接續`且目前工作者為`無`才算完成。

完成時清空本輪開始時間。形成新日誌與世代時才用實際完成時間更新最近成功交接時間；完全無變更時保留原值。

## 文字亂碼

先以UTF-8重讀；仍無法可靠解讀時停止，不得直接改寫原檔。
