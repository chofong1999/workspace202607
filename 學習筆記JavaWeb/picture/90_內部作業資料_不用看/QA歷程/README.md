# QA 歷程

此資料夾保存標註圖片的候選版本及同名 `.qa.json` 檢查紀錄。

- 每張圖片最多產生 `r01` 至 `r05`。
- 每輪都從原始截圖重新產生，不在失敗版本上繼續疊圖。
- `FAIL` 版本保留在此供追蹤。
- `PASS` 版本的 QA 紀錄保留在此，通過的圖片另行複製到樣板或正式成品資料夾。
- `QA_LIMIT_REACHED` 不代表完成，必須停止並回報未解決問題。

