# 當前圖片與批次

- 工作代號：`WRK-20260720-408T2R4T`
- 文件版本：`18`
- 當前階段：`等待使用者驗收`
- 當前任務代號：`IMG-BATCH-GATE-001`
- 最後更新：`2026-07-24T14:56:30+08:00`
- 外部規格同步狀態：`已同步`
- 交接摘要同步狀態：`已同步`

> 本文件是當前圖片與下一步的唯一正式來源。只放本次需要的短契約、例外、進度與阻塞。

## 當前目標

以 `mvproduct0721` 的 9 張非重複 JSP 新版作為批次前試作與回歸基準；內部閘門已通過，等待使用者確認圖片本身。確認前不得進入批次。

## 本張／本批次契約

- 圖片任務代號：`IMG-BATCH-GATE-001`
- 來源：`mvproduct0721` 的 `index` 1 張、`productedit` 3 張、`productform` 3 張、`productlist` 2 張有效非重複原圖；原第 3 張 `productlist` 與第 2 張同為來源第 41–87 行，已排除
- 預定成果：`9 張 JSP 標註圖`
- 成果用途：`Java Web 程式碼語言分層學習`
- 視覺規格版本：`5`
- 確認樣板版本：`無`
- 使用方案：`index.jsp 無 EL；其餘頁面本任務限定使用 CAND-EL-001／粉紅字形`
- QA層級：`製作檢查；100%／200%／400%`
- 狀態：`9 張新版均通過內部 QA，等待使用者驗收`

### 本張強制規則

- 必須完整標示 HTML、CSS、JSP 指令／JSP 註解、JSTL、JSP Action、Java scriptlet、EL 與 JavaScript；JavaScript 固定使用青色並保留圖例右欄第四列。
- 每一個 EL 必須記錄來源行號，並從各自原圖以獨立 OCR 字元框辨認 `$`、`{`、`}`及左右鄰字；不得用固定字寬公式取代。
- 粉紅染色只能改變完整 `${...}` 原始字形像素；相鄰引號、標籤、其他語法及背景的變動像素必須為 0。
- 說明對應使用同色編號；不畫跨畫面長引線，不同顏色連線交叉數必須為 0。EL、JavaScript 或 scriptlet 只有在必要時使用不碰字的短括號與短連線。
- 不同顏色的 2 px 平行框線中心距至少 4 px；安全空間不足時使用明確括號，禁止不完整矩形。
- 同一分類同一頁預設只保留一個代表編號；編號、框線、括號與短線不得互疊或過近。
- 長檔每一後續頁至少新增一行來源內容；只差游標、反白列或捲軸狀態的相同來源範圍視為重複頁並排除。
- 批次開始前必須逐項通過來源／分頁、語法分類、標記幾何、EL 獨立證據、成果分流及回歸案例六類閘門，並取得使用者對試作的確認。
- 使用者或 QA 發現新型錯誤時立即撤銷 PASS、停止批次；先補規則與可重跑檢查，再從原始基底重做並重跑全部回歸案例。

### 可調整範圍

- 說明框位置可依右側空白調整，但不得遮住程式碼或其他框線。

### 禁止改變

- 不修改或移動 SOP、原始截圖、既有標註成果、QA 及歷史資料。
- 不把 0717 的限定選擇擴寫成其他專案的永久正式 EL 方案。
- 不修改 Java Web 原始碼；不覆蓋既有原圖。

### 完成證據

- 每張成果 PNG 可開啟，且 Eclipse 目標檔案標籤可辨識。
- 語言分類、框線、說明及固定八項圖例符合視覺規格。
- 每個 EL 均保存原圖像素界證據，並確認非目標區變動像素為 0。

## 本次例外

本任務依使用者延續的染色要求使用粉紅字形；這是 `mvproduct0721` 任務限定使用，不代表跨專案唯一正式 EL 方案已選定。

## 進度、阻塞與下一步

- 已完成：`重新盤點來源行範圍後排除 1 張重複 productlist 頁面，正式待驗收成果由 10 張改為 9 張。編號互疊、同類重複編號、不完整矩形、標記碰撞、框線碰字、長引線與連線交叉均為 0；8 張含 EL 頁面共 66 個唯一 EL 保存 OCR 字元框與逐字像素證據。`
- 未完成：`尚待使用者驗收；未移入已驗收成品。`
- 阻塞：`無。`
- 下一步：`使用者檢視 mvproduct0721 未驗收資料夾內的 9 張新版；確認後才可移入已驗收成品。`

## 批次狀態

- 是否允許批次：`否`
- 批次範圍：`尚未開放；mvproduct0721 為批次前試作與回歸基準`
- 內部六類閘門：`PASS`
- 使用者試作確認：`等待確認`
- 目前唯一阻塞：`使用者尚未確認 mvproduct0721 的 9 張新版`
- 已完成：`9`
- QA未通過：`0；1 張重複頁及其他失敗輪次只保留於內部 QA 歷程`
- 等待使用者確認：`9`
- 暫停項目：`0`

## 與其他系統同步

- 外部規格最後核對：`2026-07-23T13:04:01+08:00`
- 交接系統位置：`C:\workspace202607\交接資料夾`
- 最近一次交接摘要同步：`2026-07-24T14:57:09+08:00／LOG-0027`
- 尚待同步的里程碑／阻塞：`無`
