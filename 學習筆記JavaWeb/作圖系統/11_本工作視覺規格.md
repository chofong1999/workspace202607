# 本工作視覺規格

- 工作代號：`WRK-20260720-408T2R4T`
- 視覺規格版本：`5`
- 規格狀態：`已確認`
- 主要外部規格來源ID：`SRC-SPEC-001`
- 最後更新：`2026-07-24T14:55:08+08:00`

> 本文件只保存每次作圖必須快速取得的現行摘要；完整規則以 `SRC-SPEC-001` 指向的 SOP 2.4 為準。

## 已確認強制規則

- `JWEB-RULE-001 [必須]` 目前例行標註以 JSP 為主，依畫面中的 HTML、CSS、JavaScript、Java scriptlet、JSP 指令／註解、JSP Action、JSTL 與 EL 分類。
  - 來源：`SRC-SPEC-001`
  - 適用範圍：JavaWeb 程式碼標註圖
  - 驗證方式：逐區塊與來源語法核對。
- `JWEB-RULE-002 [禁止]` 不得遮擋、裁掉、改寫或改變來源程式碼的文字與語意。
  - 來源：`SRC-SPEC-001`
  - 驗證方式：與無標註原圖比較。
- `JWEB-RULE-003 [必須]` 分類框依實際字形邊界及完整語意範圍量測；不得使用固定像素字寬推算。
  - 來源：`SRC-SPEC-001`
  - 驗證方式：檢查首末行、最長行、行尾、空白行及字形邊界。
- `JWEB-RULE-004 [必須]` 框線與連接線不得穿過字形；框選不得漏掉有效行尾，也不得包含無意義大片空白或下一個空白行。
  - 來源：`SRC-SPEC-001`
  - 驗證方式：200% 結構細節檢查。
- `JWEB-RULE-005 [必須]` 重要框選區塊具有對應的學習者說明；說明只表達程式語意，不出現製圖、像素、遮罩或 QA 內部規則。
  - 來源：`SRC-SPEC-001`
  - 驗證方式：逐一比對框選區與說明。
- `JWEB-RULE-006 [必須]` 圖例固定為兩欄八類，即使本圖未使用某一分類仍完整保留。
  - 來源：`SRC-SPEC-001`
  - 驗證方式：檢查圖例列數、順序、色塊與文字。
- `JWEB-RULE-007 [必須]` 100% 用於全圖構圖與可讀性，200% 用於結構細節，400% 用於 EL 與相鄰字元的像素級細節。
  - 來源：`SRC-SPEC-001`
  - 驗證方式：QA 紀錄分別列出三種尺寸的用途與結果。
- `JWEB-RULE-008 [必須]` 候選成果、待確認樣板、正式成果、失敗版本、歷史資料及內部 QA 依既有資料夾分流。
  - 來源：`SRC-SPEC-001`
  - 驗證方式：核對成果位置與狀態。
- `JWEB-RULE-009 [禁止]` EL 尚未選定唯一正式方案；不得把任一候選寫成已確認標準，也不得在選定前開始一般批次。
  - 來源：`SRC-SPEC-001`
  - 驗證方式：核對 `21_候選方案登錄.md`、本張契約及批次狀態。
- `JWEB-RULE-010 [必須]` 每個 EL 必須從原圖分別保存 `$`左界、`}`右界及實際字形上下界的像素證據；禁止用字元索引乘固定字寬定位，也禁止用製圖時的同一組推算座標自我驗證。
  - 來源：`SRC-SPEC-001`
  - 驗證方式：逐個 EL 核對原圖像素邊界表、左右鄰字與成果差異；缺少獨立證據不得標記 PASS。
- `JWEB-RULE-011 [必須]` 粉紅字形候選採用原圖單行 OCR 字元框建立獨立邊界證據；OCR 的 EL 數量必須與來源碼相同，且逐一辨認 `$`、`{`與`}`。辨識不成立時停止該頁，不得改用固定字寬補猜。
  - 來源：使用者確認的 `mvproduct0721` 成功方法。
  - 方法：`工作資料/方法/EL粉紅字形_OCR邊界與獨立QA.md`
  - 驗證方式：逐 EL 檢查 `$`與`}`目標前景像素全部改色、左右鄰字變動為 0、覆色遮罩外變動為 0。
- `JWEB-RULE-012 [必須]` 說明對應預設使用同色編號，不畫跨畫面長引線；不同顏色連線交叉數必須為 0。多個 EL 或無法安全畫細框的內嵌語言可使用同色括號，但只允許到說明框的短連線。
  - 來源：使用者指出交錯引線影響閱讀。
  - 方法：`工作資料/方法/說明對應_無交錯引線.md`
  - 驗證方式：100% 檢查長引線數、交叉數、編號對應與閱讀順序；200% 檢查編號及短線不碰字、不碰其他框。
- `JWEB-RULE-013 [必須]` 不同顏色的平行分類框線不得共線、重疊或貼合。兩條 2 px 框線的中心距至少 4 px，使可見線緣之間至少保留 2 px；若來源字形間沒有足夠空間，不得把框線移入字形，也不得留下看似斷線的不完整矩形，應改用同色明確括號。
  - 來源：使用者指出 HTML 與 JSTL 等內外層框線重疊。
  - 方法：`工作資料/方法/說明對應_無交錯引線.md`
  - 驗證方式：逐張掃描不同色框線的全部平行邊；共線／重疊／中心距不足 4 px 的組數必須為 0，框線穿字數仍必須為 0。
- `JWEB-RULE-014 [必須]` 同一分類在同一頁預設只放一個編號，選擇最大或最具代表性的外層區塊；其他同色框或括號以顏色對應同一說明。所有編號、框線、括號與短連線都必須做元件碰撞檢查，編號互疊、編號碰框、不同色標記碰觸及不完整矩形數均為 0。
  - 來源：使用者指出 `1／6` 編號互疊、同頁多個綠色 `4` 及 JSP 不完整框等異常。
  - 方法：`工作資料/方法/說明對應_無交錯引線.md`
  - 驗證方式：逐頁列出編號矩形與全部標記線段；同類編號數預設不超過 1，所有元件碰撞及過近項目必須為 0。
- `JWEB-RULE-015 [必須]` 長檔分頁必須記錄每張可見來源行範圍；下一頁除必要的上下文重疊外，至少新增一行尚未涵蓋的來源內容。若兩張只因游標、選取列或捲軸狀態不同而顯示相同來源範圍，後一張視為重複頁並排除，不得製作成另一張成果。
  - 來源：使用者發現 `productlist` 第 2、3 頁內容相同。
  - 方法：`工作資料/方法/長檔分頁_非重複覆蓋QA.md`
  - 驗證方式：核對來源總行數、每頁首末行、上下文重疊、新增行數及連續覆蓋；重複頁數與缺頁數均須為 0。
- `JWEB-RULE-016 [閘門]` 進入批次生產前，必須完成來源／分頁、語法分類、標記幾何、EL 獨立證據、輸出分流及回歸案例六類檢查；全部為 PASS 且當前試作已由使用者確認後，才可把 `12` 的「是否允許批次」改為是。任一項缺資料、未執行或失敗，都不得開始或繼續批次。
  - 來源：使用者要求以制度避免重犯後才進入批次。
  - 方法：`工作資料/方法/批次生產_進入閘門與回歸QA.md`
  - 驗證方式：每批 QA 必須保存六類閘門的逐項結果；不能只寫總結 PASS。
- `JWEB-RULE-017 [失敗即停]` 使用者或 QA 發現新型錯誤時，立即撤銷受影響成果的 PASS 並暫停批次；先記錄根因、把錯誤轉成可重跑的檢查項目、加入回歸清單，再從原始基底重做並重跑全部既有回歸案例。未完成規則與回歸更新前，不得只修圖後恢復生產。
  - 來源：本工作曾反覆出現 EL 邊界、背景誤染、框線、引線、編號及重複分頁等同類錯誤。
  - 方法：`工作資料/方法/批次生產_進入閘門與回歸QA.md`
  - 驗證方式：IMGLOG 必須同時記錄撤銷狀態、根因、規則／檢查更新及全回歸結果。

## 可調整範圍

- 未被 SOP、正式方案或已確認樣板固定的單張說明位置與連線路徑，可在不遮擋程式碼、不破壞閱讀順序及安全距離的範圍內調整。

## 偏好與採用頻率

- `[偏好：極高]` 使用者入口只保留可直接查看的圖片，不混入 JSON、QA、暫存或歷史資料。
- `[偏好：極高]` 說明文字直接、可驗證且面向學習者。

## 視覺元件與設計值

- 畫布／比例：以實際來源與 SOP 2.4 為準。
- 色彩：依 SOP 2.4 的分類色與候選方案；EL 色彩尚未定案。
- 字體與文字：保留來源程式碼字形；說明文字清楚可讀。
- 間距與對齊：框線、說明與圖例保持 SOP 規定的邊界與安全距離；不同色平行框線中心距至少 4 px。
- 線條、框線或圖例：圖例左欄為 HTML、CSS、JSP 指令／JSP 註解、JSTL；右欄為 JSP Action、Java scriptlet、EL、JavaScript。
- 固定元件：完整兩欄八類圖例。

## 內容與版面規則

- 內容分類：依實際 JSP 語法分類。
- 閱讀順序：程式碼區塊、對應說明及連線關係必須清楚。
- 不得遮擋內容：零文字遮擋、零框線穿字、零說明框與分類框重疊。
- 說明對應：優先使用同色編號；能用編號時不畫線。必要連線只使用短線，禁止跨畫面長斜線及不同顏色交叉。
- 框線分離：內外層或相鄰的不同色框線不得共用同一邊界；安全空間不足時使用明確括號，不得使用不完整矩形，也不得為了保留矩形而壓字或疊線。
- 編號節制：同一分類同一頁預設只保留一個代表編號；禁止重複編號造成擁擠。
- 分頁唯一性：長檔每張成果必須新增來源內容；只差游標、選取列或捲軸狀態的相同內容不得重複列為不同頁。
- 批次閘門：使用者確認試作與六類檢查全部通過前，批次狀態保持禁止。
- 錯誤回歸：新錯誤必須轉成規則與可重跑檢查；不得只修當次圖片。
- 其他：框線右界依最長行實際字形收斂；結構框跨空白行時必須有完整語意依據。

## 方案狀態

| 項目 | 正式方案 | 候選方案 | 暫停／淘汰 |
|---|---|---|---|
| EL 標示 | 無 | `CAND-EL-001`、`CAND-EL-002` | 無 |

## 檔名、分流與版本

- 檔名格式：依 SOP 2.4，例如 `檔名_sop2-4_r01.png`。
- 單張修訂格式：`r01、r02……`
- 預設自行修正上限：`5輪`
- 使用者成果入口允許內容：只允許可直接查看的圖片。
- 內部資料位置：`C:\workspace202607\學習筆記JavaWeb\picture\90_內部作業資料_不用看`

## 驗收要求

- 每張基本檢查：依 03 的 QA-BASIC、本張契約及 SOP 2.4。
- 變動檢查：依 03 的 QA-CHANGE。
- 完整檢查觸發條件：新樣板、新候選、新規則、系統性異常、最終驗收或使用者要求。
- 本工作專用驗收項目：依 SOP 2.4 執行自動檢查、100% 全圖、200% 結構及 400% EL 細節檢查；候選只使用自己的專用 QA。

## 修改權限

- AI可直接調整：已確認可調整範圍內，且不影響強制規則的單張配置。
- AI可提出但不可自行採用：新風格、新元件、新分類、新驗收條件、放寬或取代已確認規則。
- 必須先詢問使用者：會改變已確認樣板、正式方案、成果內容、使用者入口或既有成果的操作。
- 禁止：修改規則使不合格成果變成合格；把單次特例升格為長期規則。
