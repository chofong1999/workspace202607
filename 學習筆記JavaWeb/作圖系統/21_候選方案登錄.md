# 候選方案登錄

- 工作代號：`WRK-20260720-408T2R4T`
- 文件版本：`6`
- 目前候選數：`2`

> 候選不是現行正式方案，不得混入 `11` 的已確認規則，也不得直接用於一般批次。

## 進行中的候選

### CAND-EL-001

- 名稱：EL 粉紅字形
- 要解決的問題：以清楚且可重複的方式標示 JSP 中完整 `${...}` EL 語法。
- 狀態：候選測試中
- 建立來源：使用者明確要求並依 `SRC-SPEC-001` 2.4 登錄。
- 與現行方案的差異：完整 EL 原始字形本身改成粉紅色。
- 共同不變部分：來源、其他語法顏色、分類框、說明、連線、圖例與整體版型保持相同。
- 測試來源與成果：`mvweb0717/student-list.jsp`；r04 因固定字寬造成邊界錯位而撤下，現以逐 EL 原圖像素界重做 r05；使用者入口名稱為 `0717_方案A_粉紅字形_student-list.png`。
- 實作與獨立證據：依 `工作資料/方法/EL粉紅字形_OCR邊界與獨立QA.md`，從各自無標註原圖逐行取得 OCR 字元框；OCR 數量須與來源碼一致，逐一保存 `$`、`{`、`}`、完整左右界及鄰字。辨識不成立時停止，不得用固定字寬補猜。
- 專用QA：逐一檢查 `$`、`{`、內容與 `}`全部粉紅；`$`與`}`原始前景像素數須等於改色像素數；左右相鄰字元變動為 0；EL 遮罩外變動為 0；400% 檢查抗鋸齒亮度層次及白色殘影。
- 比較指標：邊界準確性、字形完整性、鄰字保全、可重複性、製作與 QA 成本。
- 已知成本與風險：像素定位可能造成漏染、誤染、白色殘影及反覆修補成本。
- 使用者決定：尚未決定。
- 影響的規則、樣板與成果：未選定前不形成正式 EL 規則、不建立已確認樣板、不啟動批次。

### CAND-EL-002

- 名稱：EL 原字＋粉紅下劃線
- 要解決的問題：在不改變 EL 原始語法顏色的情況下穩定標示完整 `${...}`。
- 狀態：候選測試中
- 建立來源：使用者明確要求並依 `SRC-SPEC-001` 2.4 登錄。
- 與現行方案的差異：保留原始字形；在完整 EL 運算式正下方畫 2 px 粉紅色水平線，水平線左右端點各向下延伸 3 px。
- 共同不變部分：來源、原始語法顏色、分類框、說明、連線、圖例與整體版型保持相同。
- 測試來源與成果：`mvweb0717/student-list.jsp`；r04 因固定字寬造成邊界錯位而撤下，現以逐 EL 原圖像素界重做 r05；使用者入口名稱為 `0717_方案B_粉紅下劃線_student-list.png`。
- 專用QA：EL 原始字形變動像素為 0；水平線粗度精確為 2 px；左右端點向下延伸長度各為 3 px，且不得向上碰觸字形；下劃線不得超過完整 `${...}` 左右界；不得包含或碰觸相鄰字元、引號、標籤、其他 EL 或下一行；400% 逐項檢查原字保全、粗度、端點長度、左右邊界及相鄰內容。
- 比較指標：原字保全、邊界準確性、辨識清楚度、視覺雜訊、可重複性、製作與 QA 成本。
- 已知成本與風險：下劃線或端點可能偏移、碰字、碰到下一行或增加視覺雜訊。
- 使用者決定：尚未決定。
- 影響的規則、樣板與成果：未選定前不形成正式 EL 規則、不建立已確認樣板、不啟動批次。

## 已結束候選索引

目前無。
